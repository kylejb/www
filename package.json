{
  "name": "portfolio",
  "author": "kylejb",
  "version": "2.0.0",
  "homepage": "https://www.kyleburda.com",
  "private": true,
  "dependencies": {
    "gh-pages": "^3.1.0",
    "next": "^10.0.7",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "styled-components": "^5.2.1",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"kyleburda.com\" >> out/CNAME && gh-pages -d out"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.12.0"
  }
}
